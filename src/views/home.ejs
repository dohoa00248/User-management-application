<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Welcome to UserManager</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: linear-gradient(to right, #6a11cb, #2575fc);
        min-height: 100vh;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
      }
      .card {
        background-color: rgba(255, 255, 255, 0.1);
        border: none;
        border-radius: 1rem;
        padding: 3rem 2rem;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
      }
      .btn-custom {
        background-color: #fff;
        color: #2575fc;
        font-weight: bold;
      }
      .btn-custom:hover {
        background-color: #e6e6e6;
        color: #2575fc;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card mx-auto" style="max-width: 500px">
        <h1 class="mb-4">Welcome to UserManager</h1>
        <p class="mb-4">
          This is a simple user management system.<br />
          Please sign in to access the admin dashboard.
        </p>
        <p>Account test: admin</p>
        <p>Password: admin</p>
        <a href="/api/v1/auth/signin" class="btn btn-custom btn-lg">Sign In</a>
      </div>
    </div>
  </body>
</html>
